# Vercel 分支特化文件：在把 main merge 到 vercel 时，自动保留 vercel(ours) 版本，避免反复冲突
package.json merge=keepvercel
package-lock.json merge=keepvercel
vercel.json merge=keepvercel

# 脚本文件：避免 bash 脚本被 CRLF 换行破坏
scripts/*.sh text eol=lf


