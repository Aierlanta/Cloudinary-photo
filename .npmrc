# NPM配置文件 - 针对Replit环境优化

# 确保在生产环境中也安装devDependencies（Replit需要）
production=false

# 使用legacy peer deps解决依赖冲突
legacy-peer-deps=true

# 禁用可选依赖以减少安装时间
optional=false

# 设置超时时间
timeout=300000

# 禁用进度条以减少日志噪音
progress=false

# 使用npm镜像（如果需要）
# registry=https://registry.npmmirror.com/
